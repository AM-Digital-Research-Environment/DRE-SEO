version: 2

updates:
  # The module ships no third-party Composer dependencies, so Actions are the only
  # moving parts worth watching.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns:
          - '*'
